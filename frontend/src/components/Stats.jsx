import { Outlet, Navigate } from "react-router-dom";

function Stats( {user} ) {
    return (
        <div>
            <h2>Stats View</h2>
            <p>Hello</p>
        </div>
    )
}

export default Stats;